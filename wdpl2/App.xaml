<?xml version="1.0" encoding="UTF-8" ?>
<Application
    x:Class="Wdpl2.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:Wdpl2.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Merge Shared Styles -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
                <ResourceDictionary Source="Resources/Styles/SharedStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <converters:IsNotNullConverter x:Key="IsNotNullConverter" />
            <converters:BoolToExpandTextConverter x:Key="BoolToExpandTextConverter" />
            <converters:IsNotNullOrEmptyConverter x:Key="IsNotNullOrEmptyConverter" />
            <converters:BoolToColorConverter x:Key="BoolToColorConverter" />

            <!-- Application-Level Button Style -->
            <Style TargetType="Button">
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="Padding" Value="12,8" />
            </Style>

            <!-- Entry Style -->
            <Style TargetType="Entry">
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>

            <!-- Editor Style -->
            <Style TargetType="Editor">
                <Setter Property="Margin" Value="0,0,0,8" />
                <Setter Property="AutoSize" Value="TextChanges" />
            </Style>

            <!-- Label Style - with proper dark mode support -->
            <Style TargetType="Label">
                <Setter Property="Margin" Value="0,4,0,2" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
            </Style>
            
            <!-- Page background for dark mode -->
            <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
