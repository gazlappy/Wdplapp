# Pool Replays

Drop exported `pool-replay-*.json` files in this folder so they show up in Solution Explorer.

These files are generated by clicking **EXPORT REC** in the pool game after recording shots.
They contain the full per-frame ball trajectories and event log for each shot.

This folder is for inspection/debugging only — files here are not embedded into the app.
