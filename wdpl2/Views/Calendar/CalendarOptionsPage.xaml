<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Wdpl2.Views.CalendarOptionsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Calendar Options">

    <!-- Split grid: 1* (categories) | 2* (editor) -->
    <Grid x:Name="RootGrid" Padding="{StaticResource LargePadding}" ColumnSpacing="{StaticResource LargeSpacing}" RowSpacing="{StaticResource MediumSpacing}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Header row -->
        <Grid Grid.ColumnSpan="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Text="Calendar Options" Style="{StaticResource PageTitleStyle}" VerticalTextAlignment="Center" />
            <HorizontalStackLayout Grid.Column="1" Spacing="8" VerticalOptions="Center">
                <Button x:Name="SaveCloseBtn" Text="Save &amp; Close" Style="{StaticResource PrimaryButtonStyle}" Clicked="OnSaveClicked" />
                <Button x:Name="ResetBtn" Text="Reset Defaults" Style="{StaticResource SecondaryButtonStyle}" Clicked="OnResetDefaultsClicked" />
            </HorizontalStackLayout>
        </Grid>

        <!-- LEFT: Option Categories -->
        <Border 
            Grid.Row="1" Grid.Column="0"
            Style="{StaticResource PanelBorderStyle}">
            <VerticalStackLayout Spacing="{StaticResource StandardSpacing}">
                <Label Text="Categories" Style="{StaticResource SubsectionHeaderStyle}" Margin="0" />
                <CollectionView
                    x:Name="CategoriesList"
                    SelectionMode="Single"
                    SelectionChanged="OnCategorySelectionChanged"
                    ItemsLayout="VerticalList">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Border
                                Padding="14,14"
                                Margin="0,0,0,4"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 8"
                                BackgroundColor="{AppThemeBinding Light={StaticResource Gray50}, Dark={StaticResource Gray800}}"
                                MinimumHeightRequest="46">
                                <Border.Triggers>
                                    <DataTrigger 
                                        TargetType="Border"
                                        Binding="{Binding Source={RelativeSource AncestorType={x:Type CollectionView}}, Path=SelectedItem}"
                                        Value="{Binding .}">
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#DBEAFE, Dark=#1E3A5F}" />
                                        <Setter Property="Stroke" Value="{StaticResource PrimaryColor}" />
                                        <Setter Property="StrokeThickness" Value="2" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Label 
                                    Text="{Binding}" 
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    VerticalTextAlignment="Center"
                                    TextColor="{AppThemeBinding Light={StaticResource Gray800}, Dark={StaticResource Gray200}}" />
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </Border>

        <!-- RIGHT: Options Editor -->
        <Border 
            Grid.Row="1" Grid.Column="1"
            Style="{StaticResource PanelBorderStyle}">
            <ScrollView>
                <ContentView x:Name="ContentPanel" Padding="4"/>
            </ScrollView>
        </Border>
    </Grid>
</ContentPage>
