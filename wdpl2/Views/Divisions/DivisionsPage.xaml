<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Wdpl2.Views.DivisionsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ctrls="clr-namespace:Wdpl2.Views.Controls"
    xmlns:models="clr-namespace:Wdpl2.Models"
    Title="Divisions"
    BackgroundColor="{AppThemeBinding Light=#F5F7F6, Dark=#111A17}"
    SemanticProperties.Description="Divisions management page">

    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,*,Auto" Padding="20" ColumnSpacing="12" RowSpacing="16">
        
        <!-- Header with Burger Menu Button -->
        <Grid Grid.ColumnSpan="2" ColumnDefinitions="Auto,*,Auto">
            <Button 
                x:Name="BurgerMenuBtn"
                Text="☰"
                FontSize="24"
                WidthRequest="50"
                HeightRequest="50"
                Padding="0"
                Margin="0,0,12,0"
                BackgroundColor="{StaticResource PrimaryColor}"
                TextColor="White"
                SemanticProperties.Description="Open menu"
                SemanticProperties.Hint="Opens the division controls menu" />
            
            <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                <Label Text="Divisions" FontSize="28" FontAttributes="Bold" />
                <Label Text="Set up divisions, season by season." FontSize="13"
                       TextColor="{AppThemeBinding Light=#52665D, Dark=#ABC1B6}" />
            </VerticalStackLayout>
            <Button x:Name="NewDivisionBtn" Grid.Column="2" Text="+ New division" Clicked="OnNewDivision"
                    BackgroundColor="#16634B" TextColor="White" VerticalOptions="Center" />
        </Grid>
        <Label x:Name="PageStatusLbl" Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource StatusTextStyle}" />

        <!-- Flyout Menu Overlay -->
        <Border 
            x:Name="FlyoutOverlay"
            Grid.Row="1"
            Grid.ColumnSpan="2"
            IsVisible="False"
            BackgroundColor="#80000000"
            ZIndex="100"
            SemanticProperties.Description="Menu overlay">
            <Border.GestureRecognizers>
                <TapGestureRecognizer x:Name="OverlayTap" />
            </Border.GestureRecognizers>
        </Border>

        <!-- Flyout Menu Panel -->
        <Border 
            x:Name="FlyoutPanel"
            Grid.Row="1"
            Grid.ColumnSpan="2"
            HorizontalOptions="Start"
            WidthRequest="400"
            IsVisible="False"
            BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1F1F1F}"
            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
            StrokeThickness="1"
            StrokeShape="RoundRectangle 0,12,12,0"
            Padding="{StaticResource LargePadding}"
            ZIndex="101"
            SemanticProperties.Description="Division controls menu">
            <ScrollView>
                <VerticalStackLayout Spacing="{StaticResource MediumSpacing}">
                    
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <Label Text="Controls" Style="{StaticResource SectionHeaderStyle}" VerticalOptions="Center" Margin="0" />
                        <Button 
                            x:Name="CloseFlyoutBtn"
                            Grid.Column="2"
                            Text="✕"
                            FontSize="18"
                            WidthRequest="40"
                            HeightRequest="40"
                            Padding="0"
                            Style="{StaticResource DangerButtonStyle}"
                            CornerRadius="20"
                            SemanticProperties.Description="Close menu" />
                    </Grid>
                    
                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}" />

                    <Label Text="Division Details" Style="{StaticResource SubsectionHeaderStyle}" Margin="0" />
                    <Label x:Name="EditorSeasonLbl" Style="{StaticResource CaptionStyle}" />

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Division Name" Style="{StaticResource FieldLabelStyle}" Margin="0,0,0,4" />
                        <Entry x:Name="NameEntry" SemanticProperties.Description="Division name" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Notes" Style="{StaticResource FieldLabelStyle}" Margin="0,0,0,4" />
                        <Editor x:Name="NotesEntry" HeightRequest="80" SemanticProperties.Description="Division notes" />
                    </VerticalStackLayout>

                    <Label Text="Actions" Style="{StaticResource SubsectionHeaderStyle}" Margin="0,8,0,0" />

                    <HorizontalStackLayout Spacing="{StaticResource StandardSpacing}">
                        <Button x:Name="AddBtn" Text="+ Add" SemanticProperties.Description="Add new division" />
                        <Button x:Name="UpdateBtn" Text="Update" SemanticProperties.Description="Update division" />
                        <Button x:Name="DeleteBtn" Text="Delete" Style="{StaticResource DangerButtonStyle}" SemanticProperties.Description="Delete division" />
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="{StaticResource StandardSpacing}">
                        <Button x:Name="MultiSelectBtn" Text="Multi-Select OFF" BackgroundColor="{StaticResource Gray500}" TextColor="White" SemanticProperties.Description="Toggle multi-select mode" />
                        <Button x:Name="BulkDeleteBtn" Text="Delete Selected" Style="{StaticResource DangerButtonStyle}" IsVisible="False" SemanticProperties.Description="Delete selected divisions" />
                    </HorizontalStackLayout>

                    <Label Text="Data Management" Style="{StaticResource SubsectionHeaderStyle}" Margin="0,8,0,0" />

                    <HorizontalStackLayout Spacing="{StaticResource StandardSpacing}">
                        <Button x:Name="ReloadBtn" Text="Reload" SemanticProperties.Description="Reload data" />
                    </HorizontalStackLayout>

                    <Button x:Name="ExportBtn" Text="Export CSV" SemanticProperties.Description="Export divisions to CSV" />

                    <ctrls:CsvImportBox x:Name="DivisionsImport" Title="Import Divisions (.csv)" SemanticProperties.Description="Import divisions from CSV" />
                    
                    <Button x:Name="DebugCheckBtn" Text="🔍 Check Database" BackgroundColor="#FF6B00" TextColor="White" SemanticProperties.Description="Check database integrity" />

                    <Label x:Name="StatusLbl" Text="" Style="{StaticResource StatusTextStyle}" SemanticProperties.Description="Status message" />
                    
                </VerticalStackLayout>
            </ScrollView>
        </Border>

        <!-- Left: search + list -->
        <Border 
            Grid.Row="1" 
            StrokeShape="RoundRectangle 12" 
            Padding="{StaticResource MediumPadding}"
            BackgroundColor="{AppThemeBinding Light=#FAFAFA, Dark=#1A1A1A}"
            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
            StrokeThickness="1"
            SemanticProperties.Description="Divisions list panel">
            <Grid RowDefinitions="Auto,Auto,Auto,*" RowSpacing="{StaticResource StandardSpacing}">
                <VerticalStackLayout Spacing="4">
                    <Label Text="Season to configure" FontAttributes="Bold" />
                    <Picker x:Name="ConfigurationSeasonPicker" Title="Choose a season"
                            ItemDisplayBinding="{Binding Name}" SelectedIndexChanged="OnConfigurationSeasonChanged"
                            SemanticProperties.Description="Season to configure without activating it" />
                    <Label x:Name="SeasonContextLbl" FontSize="12" />
                    <Label Text="Includes new, inactive seasons. Choosing here does not change your working season."
                           Style="{StaticResource CaptionStyle}" />
                </VerticalStackLayout>
                <Entry x:Name="SearchEntry" Grid.Row="1" Placeholder="Search divisions..." SemanticProperties.Description="Search divisions" />
                
                <HorizontalStackLayout Grid.Row="2" Spacing="8" Padding="4,0">
                    <CheckBox x:Name="ShowAllSeasonsCheck" SemanticProperties.Description="Show divisions from all seasons" />
                    <Label 
                        Text="Browse all seasons (read-only)"
                        VerticalOptions="Center"
                        FontSize="14">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnShowAllSeasonsTapped" />
                        </Label.GestureRecognizers>
                    </Label>
                </HorizontalStackLayout>
                
                <CollectionView 
                    x:Name="DivisionsList" 
                    Grid.Row="3"
                    SelectionMode="Single"
                    VerticalScrollBarVisibility="Always"
                    SemanticProperties.Description="Divisions list">
                    <CollectionView.EmptyView>
                        <Label Text="No divisions found. Choose a season above, then select + New division to get started."
                               Padding="16" Style="{StaticResource CaptionStyle}" />
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Division">
                            <Border
                                Padding="{StaticResource StandardPadding}"
                                Margin="0,0,0,4"
                                BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#2A2A2A}"
                                Stroke="Transparent"
                                StrokeThickness="2"
                                StrokeShape="RoundRectangle 8">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroupList>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="Selected">
                                                <VisualState.Setters>
                                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#DBEAFE, Dark=#1E3A5F}" />
                                                    <Setter Property="Stroke" Value="{AppThemeBinding Light=#3B82F6, Dark=#60A5FA}" />
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateGroupList>
                                </VisualStateManager.VisualStateGroups>
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource StandardSpacing}">
                                    <Label 
                                        Text="{Binding Name}" 
                                        FontSize="14" 
                                        FontAttributes="Bold"
                                        VerticalOptions="Center" />
                                    <Label 
                                        Grid.Column="1" 
                                        Text="{Binding Notes}" 
                                        Style="{StaticResource CaptionStyle}"
                                        VerticalOptions="Center"
                                        MaxLines="1"
                                        LineBreakMode="TailTruncation" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>
        </Border>

        <!-- Right: Division Details Panel - Enhanced -->
        <Border 
            Grid.Row="1" 
            Grid.Column="1"
            StrokeShape="RoundRectangle 12" 
            Padding="{StaticResource MediumPadding}"
            BackgroundColor="{AppThemeBinding Light=#FAFAFA, Dark=#1A1A1A}"
            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
            StrokeThickness="1"
            SemanticProperties.Description="Division details panel">
            <ScrollView>
                <VerticalStackLayout Spacing="{StaticResource MediumSpacing}">
                    
                    <!-- Empty State -->
                    <VerticalStackLayout 
                        x:Name="EmptyStatePanel"
                        Spacing="16"
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        Padding="40"
                        IsVisible="True"
                        SemanticProperties.Description="No division selected">
                        <Label 
                            Text="📊"
                            FontSize="48"
                            HorizontalOptions="Center" />
                        <Label 
                            Text="Select a division"
                            FontSize="18"
                            FontAttributes="Bold"
                            TextColor="{StaticResource Gray500}"
                            HorizontalOptions="Center" />
                        <Label 
                            Text="Click a division from the list to view details, statistics, and manage teams"
                            Style="{StaticResource CaptionStyle}"
                            HorizontalOptions="Center"
                            HorizontalTextAlignment="Center"
                            MaximumWidthRequest="300" />
                    </VerticalStackLayout>

                    <!-- Division Info Panel (initially hidden) -->
                    <VerticalStackLayout x:Name="DivisionInfoPanel" IsVisible="False" Spacing="16" SemanticProperties.Description="Selected division details">
                        
                        <!-- Header with division name -->
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Spacing="4">
                                <Label x:Name="SelectedDivisionName" FontSize="22" FontAttributes="Bold" />
                                <Label x:Name="SelectedDivisionSeason" Style="{StaticResource CaptionStyle}" />
                            </VerticalStackLayout>
                            <Border
                                Grid.Column="1"
                                BackgroundColor="{StaticResource PrimaryColor}"
                                StrokeThickness="0"
                                Padding="12,6"
                                StrokeShape="RoundRectangle 16"
                                VerticalOptions="Start">
                                <Label 
                                    x:Name="SelectedDivisionTeamCount"
                                    FontSize="13"
                                    FontAttributes="Bold"
                                    TextColor="White" />
                            </Border>
                        </Grid>

                        <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" />
                        
                        <!-- Stats Cards -->
                        <Label Text="STATISTICS" Style="{StaticResource CaptionStyle}" FontAttributes="Bold" />
                        <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="12">
                            
                            <!-- Teams Card -->
                            <Border 
                                BackgroundColor="{AppThemeBinding Light=#EFF6FF, Dark=#1E3A5F}"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 12"
                                Padding="16">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="🏆" FontSize="24" />
                                    <Label x:Name="DivisionTeamCountStat" FontSize="28" FontAttributes="Bold" TextColor="{StaticResource PrimaryColor}" />
                                    <Label Text="Teams" Style="{StaticResource CaptionStyle}" />
                                </VerticalStackLayout>
                            </Border>
                            
                            <!-- Players Card -->
                            <Border 
                                Grid.Column="1"
                                BackgroundColor="{AppThemeBinding Light=#F0FDF4, Dark=#14532D}"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 12"
                                Padding="16">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="🎱" FontSize="24" />
                                    <Label x:Name="DivisionPlayerCountStat" FontSize="28" FontAttributes="Bold" TextColor="#16A34A" />
                                    <Label Text="Players" Style="{StaticResource CaptionStyle}" />
                                </VerticalStackLayout>
                            </Border>
                            
                            <!-- Fixtures Card -->
                            <Border 
                                Grid.Row="1"
                                BackgroundColor="{AppThemeBinding Light=#FEF3C7, Dark=#713F12}"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 12"
                                Padding="16">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="📅" FontSize="24" />
                                    <Label x:Name="DivisionFixtureCountStat" FontSize="28" FontAttributes="Bold" TextColor="#CA8A04" />
                                    <Label Text="Fixtures" Style="{StaticResource CaptionStyle}" />
                                </VerticalStackLayout>
                            </Border>
                            
                            <!-- Completed Card -->
                            <Border 
                                Grid.Row="1"
                                Grid.Column="1"
                                BackgroundColor="{AppThemeBinding Light=#FCE7F3, Dark=#831843}"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 12"
                                Padding="16">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="✓" FontSize="24" />
                                    <Label x:Name="DivisionCompletedStat" FontSize="28" FontAttributes="Bold" TextColor="#DB2777" />
                                    <Label Text="Completed" Style="{StaticResource CaptionStyle}" />
                                </VerticalStackLayout>
                            </Border>
                        </Grid>
                        
                        <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" />
                        
                        <!-- Teams List -->
                        <Label Text="TEAMS IN THIS DIVISION" Style="{StaticResource CaptionStyle}" FontAttributes="Bold" />
                        <Border
                            StrokeShape="RoundRectangle 8"
                            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                            StrokeThickness="1"
                            BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource Gray800}}"
                            HeightRequest="180">
                            <CollectionView x:Name="DivisionTeamsDisplay" VerticalScrollBarVisibility="Always" SemanticProperties.Description="Teams in this division">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="12,8" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                                            <Border
                                                BackgroundColor="{StaticResource PrimaryColor}"
                                                WidthRequest="36"
                                                HeightRequest="36"
                                                StrokeThickness="0"
                                                StrokeShape="RoundRectangle 18">
                                                <Label 
                                                    Text="🏆" 
                                                    FontSize="16"
                                                    HorizontalOptions="Center"
                                                    VerticalOptions="Center" />
                                            </Border>
                                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="2">
                                                <Label Text="{Binding Name}" FontSize="14" FontAttributes="Bold" />
                                                <Label Text="{Binding VenueName}" Style="{StaticResource CaptionStyle}" />
                                            </VerticalStackLayout>
                                            <Label 
                                                Grid.Column="2" 
                                                Text="{Binding PlayerCount, StringFormat='{0} players'}" 
                                                Style="{StaticResource CaptionStyle}"
                                                VerticalOptions="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                                <CollectionView.EmptyView>
                                    <VerticalStackLayout Padding="20" HorizontalOptions="Center">
                                        <Label Text="No teams in this division" Style="{StaticResource CaptionStyle}" HorizontalOptions="Center" />
                                        <Label Text="Add teams via the Teams page" Style="{StaticResource CaptionStyle}" HorizontalOptions="Center" />
                                    </VerticalStackLayout>
                                </CollectionView.EmptyView>
                            </CollectionView>
                        </Border>
                        
                        <!-- Recent Fixtures -->
                        <Label Text="RECENT FIXTURES" Style="{StaticResource CaptionStyle}" FontAttributes="Bold" Margin="0,8,0,0" />
                        <Border
                            StrokeShape="RoundRectangle 8"
                            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                            StrokeThickness="1"
                            BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource Gray800}}"
                            HeightRequest="120">
                            <CollectionView x:Name="DivisionFixturesDisplay" VerticalScrollBarVisibility="Always" SemanticProperties.Description="Recent fixtures in this division">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="12,8" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                                            <Label 
                                                Text="{Binding Date, StringFormat='{0:dd MMM}'}" 
                                                FontSize="11"
                                                FontAttributes="Bold"
                                                TextColor="{StaticResource Gray500}"
                                                VerticalOptions="Center"
                                                WidthRequest="50" />
                                            <Label 
                                                Grid.Column="1" 
                                                Text="{Binding MatchTitle}" 
                                                FontSize="13" 
                                                VerticalOptions="Center"
                                                LineBreakMode="TailTruncation" />
                                            <Label 
                                                Grid.Column="2" 
                                                Text="{Binding ScoreText}" 
                                                FontSize="13"
                                                FontAttributes="Bold"
                                                TextColor="{StaticResource PrimaryColor}"
                                                VerticalOptions="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                                <CollectionView.EmptyView>
                                    <Label Text="No fixtures scheduled" Style="{StaticResource CaptionStyle}" Padding="20" HorizontalOptions="Center" />
                                </CollectionView.EmptyView>
                            </CollectionView>
                        </Border>
                        
                        <!-- Notes Section -->
                        <VerticalStackLayout x:Name="NotesSection" IsVisible="False" Spacing="4">
                            <Label Text="NOTES" Style="{StaticResource CaptionStyle}" FontAttributes="Bold" />
                            <Border
                                StrokeShape="RoundRectangle 8"
                                Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                                StrokeThickness="1"
                                BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource Gray800}}"
                                Padding="12">
                                <Label x:Name="SelectedDivisionNotes" Style="{StaticResource CaptionStyle}" />
                            </Border>
                        </VerticalStackLayout>

                    </VerticalStackLayout>

                </VerticalStackLayout>
            </ScrollView>
        </Border>
    </Grid>
</ContentPage>