<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Wdpl2.Views.CompetitionsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:Wdpl2.Models"
    xmlns:viewmodels="clr-namespace:Wdpl2.ViewModels"
    x:DataType="viewmodels:CompetitionsViewModel"
    Title="Competitions">

    <ContentPage.Resources>
        <Style TargetType="Frame">
            <Setter Property="HasShadow" Value="False"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}"/>
        </Style>
        <Style TargetType="Label" x:Key="SectionTitle">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Margin" Value="8,0,0,8"/>
        </Style>
        <Style TargetType="Label" x:Key="FieldLabel">
            <Setter Property="VerticalTextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>
    </ContentPage.Resources>

    <!-- Split grid: 1* (competitions list) | 2* (competition details) -->
    <Grid x:Name="RootGrid" Padding="12" ColumnSpacing="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: Competitions List -->
        <Frame Grid.Column="0">
            <VerticalStackLayout Spacing="8">
                <Label Text="Competitions" Style="{StaticResource SectionTitle}" />
                
                <!-- Active / History Toggle -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="4" Margin="0,0,0,4">
                    <Button 
                        x:Name="ActiveTab" 
                        Text="Active"
                        Clicked="OnShowActive"
                        BackgroundColor="#3B82F6"
                        TextColor="White"
                        CornerRadius="8"
                        Padding="6,4" FontSize="13" />
                    <Button 
                        x:Name="HistoryTab" 
                        Grid.Column="1"
                        Text="History"
                        Clicked="OnShowHistory"
                        BackgroundColor="#6B7280"
                        TextColor="White"
                        CornerRadius="8"
                        Padding="6,4" FontSize="13" />
                </Grid>
                
                <!-- Competition List -->
                <ScrollView HeightRequest="400" x:Name="CompetitionsListScroll">
                    <VerticalStackLayout x:Name="CompetitionsList" Spacing="4">
                    </VerticalStackLayout>
                </ScrollView>

                <!-- Action Buttons -->
                <HorizontalStackLayout Spacing="8" Margin="0,8,0,0">
                    <Button 
                        x:Name="NewBtn"
                        Text="New"
                        Clicked="OnNewCompetition"
                        HorizontalOptions="FillAndExpand" />
                    <Button 
                        x:Name="DeleteBtn"
                        Text="Delete"
                        Clicked="OnDeleteCompetition"
                        BackgroundColor="#EF4444"
                        TextColor="White"
                        HorizontalOptions="FillAndExpand" />
                </HorizontalStackLayout>

                <!-- Status Label -->
                <Label 
                    x:Name="StatusLbl"
                    Text="{Binding StatusMessage}"
                    FontSize="11"
                    TextColor="Gray"
                    Margin="0,8,0,0"
                    HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Frame>

        <!-- RIGHT: Competition Details/Editor -->
        <Frame Grid.Column="1">
            <ScrollView>
                <ContentView x:Name="ContentPanel" Padding="12"/>
            </ScrollView>
        </Frame>
    </Grid>
</ContentPage>
